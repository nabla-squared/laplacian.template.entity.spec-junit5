rootProject.name = "laplacian.template.entity.spec-junit5"

pluginManagement {
    repositories {
        maven(url = "../mvn-repo/")
        maven(url = "https://raw.github.com/nabla-squared/mvn-repo/master/")
        gradlePluginPortal()
        jcenter()
    }
}

// @subprojects@
// Subproject definitions are inserted here.
// @subprojects@